PORT=5000
DATABASE_URL="mysql://root@localhost:3306/marjane_wallet"
JWT_SECRET="change-this-to-a-random-secret-min-32-chars!!"
JWT_REFRESH_SECRET="change-this-refresh-secret-min-32-chars-!!!!!!!"
NODE_ENV=development

# SMTP (optional — emails are logged to console when unset)
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
